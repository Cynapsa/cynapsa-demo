# E2E Fixtures

`traceability_v1.json` is the machine-readable public V1 matrix. Each row
binds a requirement to a positive scenario, required rejection, executable
test, environment profile, exact public expectation, observed candidate
result, and sanitized artifact name.

All fixture values are deterministic and synthetic. No production endpoint,
credential, payload, private identifier, or dependency error belongs here.
