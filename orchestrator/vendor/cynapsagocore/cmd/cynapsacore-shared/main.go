// Package main will build the native shared library consumed by generated SDK bindings.
package main

// main intentionally remains empty: SDK callers explicitly create and own
// every Core through the native ABI.
func main() {}
